extends layouts/main
include mixins/videoBlock

block content
    .search__header
        span Searching for:
            b  #{searchingBy}
    .search__videos
        if videos.length === 0
            h5 No Videos Found
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl,
                id:item.id
            })